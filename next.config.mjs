/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["res.cloudinary.com", "via.placeholder.com", "dentistry.co.uk"],
  },
  reactStrictMode: false,
};

export default nextConfig;
